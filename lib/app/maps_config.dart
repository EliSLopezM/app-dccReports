/// RF-7: mientras no haya una API key real de Google Maps configurada,
/// `MapView` muestra un placeholder en vez del mapa real. Cambiar a
/// `true` una vez agregada la key (ver `lineamientos/entorno-local.md`).
const bool kGoogleMapsConfigured = false;
