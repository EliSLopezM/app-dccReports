import 'package:flutter/material.dart';

/// Contenido real: spec futura (docs/brief-inicial.md, spec 008).
class PreparateStubScreen extends StatelessWidget {
  const PreparateStubScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Prepárate')),
      body: const Center(child: Text('Próximamente')),
    );
  }
}
