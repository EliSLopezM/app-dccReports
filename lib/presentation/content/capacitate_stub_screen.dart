import 'package:flutter/material.dart';

/// Contenido real: spec futura (docs/brief-inicial.md, spec 008).
class CapacitateStubScreen extends StatelessWidget {
  const CapacitateStubScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Capacítate')),
      body: const Center(child: Text('Próximamente')),
    );
  }
}
